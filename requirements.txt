llama-index-core>=0.11.0
llama-index-vector-stores-postgres>=0.2.0

# Embeddings -- Local HF embedding model
llama-index-embeddings-huggingface>=0.3.0

# Generation + judge LLM
llama-index-llms-anthropic>=0.4.0
# Pinned below 1.0: anthropic SDK v1.0 (Aug 2026) dropped the temperature/
# top_p/top_k kwargs from Messages.create(), which llama-index-llms-anthropic
# (as of 0.11.10) still passes -> TypeError. Unpin once a llama-index-llms-anthropic
# release supports the v1.x anthropic SDK.
anthropic>=0.40.0,<1.0.0

python-dotenv>=1.0.0
psycopg2-binary>=2.9.0

# Reranker (cross-encoder postprocessor, phase 5)
sentence-transformers>=2.7.0
 
# PPDM source parsing (parsers.py): pdfplumber pulls words from the event
# catalog PDF in content-stream order (use_text_flow=True) so the
# Severity/Category/Message-ID row structure survives extraction. The SOP
# docx side uses the external `pandoc` CLI instead (not pip-installable --
# see README.md), so no package is needed for that half.
pdfplumber>=0.11.0

# Orchestration: serving graph + eval graph (phases 3-6)
langgraph>=0.2.0

# Service boundary (phase 6 / FastAPI endpoints throughout)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Statistical eval tooling: bootstrap CIs, paired significance tests,
scipy>=1.11.0
numpy>=1.24.0